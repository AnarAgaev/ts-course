// Type or alias
type MyBoolean = false | true

type Pair = [string, string]
type Pairs = Pair[]
const pairMatrix: Pairs[] = []

type Machine = {
    brand: 'string'
    type: 'string'
    dors: number
    free: boolean
}
const cars: Machine[] = []